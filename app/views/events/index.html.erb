<p id="notice"><%= notice %></p>

<h1>Evénements :</h1>

<h2>Voici la liste des événements que nous proposons :</h2>



<h4>Voici les événements en cours :</h4>

<table>
  <thead>
  <tr>
    <th>Nom</th>
    <th>Date de début</th>
    <th>Date de fin</th>
    <th>Lieux</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @events.each do |event| %>
    <tr>
      <%  if event.startDate < @date && event.endDate > @date %>
        <td><%= event.name %></td>
        <td><%= event.startDate %></td>
        <td><%= event.endDate %></td>
        <td><%= event.eventPlace %></td>
        <td><%= link_to 'Voir l\'événement', event %></td>
      <%  end %>
    </tr>
  <% end %>
  </tbody>
</table>

<h4>Voici les événements à venir :</h4>
<table>
  <thead>
  <tr>
    <th>Nom</th>
    <th>Date de début</th>
    <th>Date de fin</th>
    <th>Lieux</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @events.each do |event| %>
    <tr>
      <% if event.startDate > @date %>
        <td><%= event.name %></td>
        <td><%= event.startDate %></td>
        <td><%= event.endDate %></td>
        <td><%= event.eventPlace %></td>
        <td><%= link_to 'Voir l\'événement', event %></td>

      <% end %>
    </tr>
  <% end %>
  </tbody>
</table>

<h4>Voici les événements déja terminés :</h4>
<table>
  <thead>
  <tr>
    <th>Nom</th>
    <th>Date de début</th>
    <th>Date de fin</th>
    <th>Lieux</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @events.each do |event| %>
    <tr>
      <% if event.startDate < @date && event.endDate < @date %>
        <td><%= event.name %></td>
        <td><%= event.startDate %></td>
        <td><%= event.endDate %></td>
        <td><%= event.eventPlace %></td>
        <td><%= link_to 'Voir l\'événement', event %></td>
      <% end %>
    </tr>
  <% end %>
  </tbody>
</table>


<br>

<%= link_to 'New Event', new_event_path %>
